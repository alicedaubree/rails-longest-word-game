<h2 class="mt-5 mb-5 text center">Result</h2>

<% if @result == true && @valid == true && @guess.size <11 %>
  <p>Congatulations! <strong><%=@guess%></strong> is a valid English word</p>
<% elsif @result == false && @valid == true && @guess.size < 11 %>
   <p>Sorry but <strong><%=@guess%></strong> does not seem to be a valid English World</p>
<% else %><p>Sorry but <strong><%=@guess%></strong> can't be build with <%=@letters_array.join(", ")%></p>
<% end%>
<%= link_to "Try again !", new_path %>
